<h1>Пользователи</h1>

<%= paginate @users %>

<table>
  <tr>
    <th>Имя</th>
    <th>Email</th>
    <th></th>
  </tr>

<% @users.each do |user| %>
  <tr>
    <td><%= user.name %></td>
    <td><%= user.email %></td>
    <td><%= link_to 'Обзор', user %></td>
  </tr>
<% end %>
</table>

